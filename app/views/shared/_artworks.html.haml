.m-gallery
  - artworks.each do |artwork|
    .m-gallery__piece
      = link_to artwork do
        = image_tag artwork.image.url(:square_small)
        %h2= artwork.name

        .u-center#likes_count= render partial: 'artworks/like_link', locals: { subject: artwork, user_list: true }
