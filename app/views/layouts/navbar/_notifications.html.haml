.m-notifications.js-notifications-window
  .m-notifications__menu
    Notifications
    = link_to mark_all_read_path, remote: true do
      .u-small-text Mark all as read
  %hr.u-black-line--thin
  - if current_user.notifications.any?
    - current_user.notifications.order('created_at desc').take(10).each do |notification|
      = render partial: 'notifications/notification', locals: { notification: notification }

    .u-center
      = link_to 'Show all', notifications_path if current_user.notifications.size >= 10
  - else
    You have no notifications yet 😕
