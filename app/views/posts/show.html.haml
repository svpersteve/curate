- set_page_title(@post.title)

.container
  .content.content--xxwide.u-center
    %h1.a-headline
      = post_status(@post)
      = @post.title
    .u-center#likes_count= render partial: 'shared/like_link', locals: { subject: @post, user_list: true }

.container
  .content.content--tight
    %h4
      = link_to "By #{@post.author.full_name}", @post.author
      %span.u-float-right
        = post_date(@post)
    = link_to 'Edit', edit_post_path(@post), class: 'a-button a-button--corner' if can? :edit, @post
    = sanitize @post.content

    .u-center
      %h1#likes_icon= render partial: 'shared/like_link', locals: { subject: @post }

- if @post.tags.any?
  .container
    .content
      %h3 Tags
      = render partial: 'shared/tags', locals: { tags: @post.tags }
