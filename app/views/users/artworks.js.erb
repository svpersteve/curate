$('.is-selected').removeClass('is-selected')
$('#artwork-nav').addClass('is-selected')
$("#profile-ajax").html("<%= escape_javascript(render partial: 'users/profile/artworks', locals: { user: @user }) %>");
