- set_page_title(@user.username)

.container
  .content
    = profile_image(@user)
    %h1= @user.full_name
    %p= instagram_link(@user)
    %p= @user.bio

- if can? :manage, @user
  .container
    .content= link_to 'Edit profile', edit_user_path(@user), class: 'a-button a-button--corner'
