$('.is-selected').removeClass('is-selected')
$('#post-nav').addClass('is-selected')
$("#profile-ajax").html("<%= escape_javascript(render partial: 'users/profile/posts', locals: { user: @user }) %>");
